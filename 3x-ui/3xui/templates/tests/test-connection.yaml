apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "3xui.fullname" . }}-test-connection"
  labels:
    {{- include "3xui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "3xui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
